// EXPECT: 1
fun main(): i32 {
    let c: char = 'A';
    let s: char* = "test";
    
    // Test that char comparison works
    if (c == 'A') {
        return 1;
    }

    // NOTE: This is like this because windows just doesn't like casting char to i32. :shrug:
    
    return 0;
}
